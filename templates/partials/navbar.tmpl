{{define "navbar"}}
<nav class="bg-white shadow-md">
    <div class="container mx-auto px-4">
        <div class="flex justify-between items-center py-4">
            <div class="flex items-center space-x-4">
                <a href="/" class="text-2xl font-bold text-blue-600">SocGo</a>
            </div>
            
            <div class="flex items-center space-x-6">
                <a href="/dashboard" class="{{if eq .CurrentPage "dashboard"}}text-blue-600 font-semibold{{else}}text-gray-600 hover:text-blue-600{{end}} transition-colors">
                    Dashboard
                </a>
                <a href="/providers" class="{{if eq .CurrentPage "providers"}}text-blue-600 font-semibold{{else}}text-gray-600 hover:text-blue-600{{end}} transition-colors">
                    Providers
                </a>
                <a href="/posts" class="{{if eq .CurrentPage "posts"}}text-blue-600 font-semibold{{else}}text-gray-600 hover:text-blue-600{{end}} transition-colors">
                    Posts
                </a>
                <a href="/calendar" class="{{if eq .CurrentPage "calendar"}}text-blue-600 font-semibold{{else}}text-gray-600 hover:text-blue-600{{end}} transition-colors">
                    Calendar
                </a>
            </div>
        </div>
    </div>
</nav>
{{end}}